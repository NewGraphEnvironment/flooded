% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fl_valley_poly.R
\name{fl_valley_poly}
\alias{fl_valley_poly}
\title{Convert a binary valley raster to polygons}
\usage{
fl_valley_poly(x, dissolve = TRUE)
}
\arguments{
\item{x}{A \code{SpatRaster} with binary values (\code{0}/\code{1}), typically the output
of \code{\link[=fl_valley_confine]{fl_valley_confine()}}.}

\item{dissolve}{Logical. If \code{TRUE} (default), adjacent valley cells are
dissolved into multipolygon features. If \code{FALSE}, each contiguous patch
is a separate polygon.}
}
\value{
An \code{sf} polygon object in the CRS of \code{x}. Contains a \code{valley}
column with value \code{1}.
}
\description{
Polygonizes a binary (0/1) valley raster, returning an \code{sf} polygon layer.
Valley cells (\code{1}) are dissolved into contiguous polygons; non-valley cells
are dropped. This is the natural final step after \code{\link[=fl_valley_confine]{fl_valley_confine()}}.
}
\examples{
dem <- terra::rast(system.file("testdata/dem.tif", package = "flooded"))
slope <- terra::rast(system.file("testdata/slope.tif", package = "flooded"))
gentle <- fl_mask(slope, threshold = 9, operator = "<=")
poly <- fl_valley_poly(gentle)
terra::plot(dem, main = "Gentle slopes as polygons")
plot(sf::st_geometry(poly), add = TRUE, col = "#00800040", border = "darkgreen")

}
