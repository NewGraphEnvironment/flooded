% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fl_stream_rasterize.R
\name{fl_stream_rasterize}
\alias{fl_stream_rasterize}
\title{Rasterize a stream network onto a DEM grid}
\usage{
fl_stream_rasterize(streams, template, field = "channel_width")
}
\arguments{
\item{streams}{An \code{sf} linestring object with the stream network.}

\item{template}{A \code{SpatRaster} that defines the output grid (extent, resolution,
CRS). Typically the DEM.}

\item{field}{Character. Column name in \code{streams} to use as the cell value.
Must be numeric. Default \code{"channel_width"}.}
}
\value{
A \code{SpatRaster} with the same grid as \code{template}. Stream cells carry
the value of \code{field}; all other cells are \code{NA}.
}
\description{
Burns stream line features onto the grid defined by a template raster.
Each stream cell receives the value of \code{field} (typically upstream
contributing area or channel width); non-stream cells are \code{NA}.
}
\details{
Rasterization uses \code{\link[terra:rasterize]{terra::rasterize()}} with \code{touches = FALSE} (only cells
whose centre falls on a stream line are burned). When multiple features
overlap a cell, the maximum value is kept.

The output CRS matches \code{template}. If \code{streams} and \code{template} have
different CRS, \code{streams} is reprojected to match.
}
