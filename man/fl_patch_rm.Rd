% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fl_patch_rm.R
\name{fl_patch_rm}
\alias{fl_patch_rm}
\title{Remove small patches from a binary raster}
\usage{
fl_patch_rm(x, min_area, directions = 4L)
}
\arguments{
\item{x}{A \code{SpatRaster} with binary values (\code{0}/\code{1}).}

\item{min_area}{Numeric. Minimum patch area in map units squared (e.g., m²).
Patches smaller than this are removed.}

\item{directions}{Integer. \code{4} for rook connectivity, \code{8} for queen.
Default \code{4} (matches VCA 4-connectivity convention).}
}
\value{
A \code{SpatRaster} with the same grid as \code{x}. Small patches are set to
\code{0}; all other values are unchanged.
}
\description{
Identifies connected patches of \code{1}-valued cells and removes (sets to \code{0})
any patch whose area is below a size threshold.
}
