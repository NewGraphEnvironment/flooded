% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fl_mask.R
\name{fl_mask}
\alias{fl_mask}
\title{Create a binary mask from a raster by thresholding}
\usage{
fl_mask(x, threshold, operator = "<=")
}
\arguments{
\item{x}{A \code{SpatRaster} with numeric values.}

\item{threshold}{Numeric scalar. The threshold value.}

\item{operator}{Character. One of \code{"<="}, \code{"<"}, \code{">="}, \code{">"}, \code{"=="},
\code{"!="}. Default \code{"<="}.}
}
\value{
A \code{SpatRaster} with values \code{1} (condition met) and \code{0} (condition
not met). \code{NA} cells in \code{x} remain \code{NA}.
}
\description{
Applies a comparison operator and threshold to a raster, returning a binary
(0/1) mask. Useful for slope masks, elevation masks, depth masks, etc.
}
\examples{
\dontrun{
slope <- terra::terrain(dem, "slope", unit = "percent")
gentle <- fl_mask(slope, threshold = 9, operator = "<=")
}

}
