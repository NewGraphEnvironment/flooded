% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fl_flood_model.R
\name{fl_flood_model}
\alias{fl_flood_model}
\title{Run the full flood model}
\usage{
fl_flood_model(dem, streams, flood_factor = 6, precip = NULL, max_width = 2000)
}
\arguments{
\item{dem}{A \code{SpatRaster} of elevation.}

\item{streams}{A \code{SpatRaster} of rasterized streams (output of
\code{\link[=fl_stream_rasterize]{fl_stream_rasterize()}}). Cell values \strong{must be upstream contributing
area in hectares} — they are the drainage-area term of the bankfull
regression, not a generic channel-size proxy. Converted to km2 internally.}

\item{flood_factor}{Numeric. Multiplier on bankfull depth to estimate flood
depth. Default \code{6} (VCA convention).}

\item{precip}{A \code{SpatRaster} of mean annual precipitation in \strong{millimetres},
or a single numeric value in mm applied uniformly. Converted to cm/yr
internally. Default \code{NULL}, which drops the precipitation term.}

\item{max_width}{Numeric. Maximum corridor width in map units (metres)
within which to interpolate. Default \code{2000} (1000m each side).}
}
\value{
A \code{SpatRaster} with three layers:
\describe{
\item{flood_surface}{Water surface elevation at stream cells (\code{NA} elsewhere).}
\item{flood_depth}{Depth above terrain (metres). \code{0} at streams, \code{NA} where not flooded.}
\item{flooded}{Binary mask: \code{1} where \code{flood_depth > 0}, \code{0} at streams, \code{NA} elsewhere.}
}
}
\description{
Convenience wrapper that calls \code{\link[=fl_flood_surface]{fl_flood_surface()}} and \code{\link[=fl_flood_depth]{fl_flood_depth()}}
in sequence, returning a multi-layer \code{SpatRaster} with the flood surface
elevation, flood depth, and a binary flooded mask.
}
\examples{
dem <- terra::rast(system.file("testdata/dem.tif", package = "flooded"))
streams <- sf::st_read(
  system.file("testdata/streams.gpkg", package = "flooded"),
  quiet = TRUE
)
stream_r <- fl_stream_rasterize(streams, dem, field = "upstream_area_ha")
precip_r <- fl_stream_rasterize(streams, dem, field = "map_upstream")
flood <- fl_flood_model(dem, stream_r, flood_factor = 6, precip = precip_r)

# Three layers: flood_surface, flood_depth, flooded
names(flood)
terra::plot(flood[["flood_depth"]], main = "Flood depth (m)")

}
