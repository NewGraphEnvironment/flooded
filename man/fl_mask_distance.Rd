% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fl_mask_distance.R
\name{fl_mask_distance}
\alias{fl_mask_distance}
\title{Binary mask by Euclidean distance from features}
\usage{
fl_mask_distance(x, threshold)
}
\arguments{
\item{x}{A \code{SpatRaster} where non-\code{NA} cells are the features to measure
distance from (e.g., rasterized streams).}

\item{threshold}{Numeric. Maximum distance in map units (e.g., metres).
Cells within this distance are \code{1}; cells beyond are \code{0}.}
}
\value{
A \code{SpatRaster} with values \code{1} (within threshold) and \code{0} (beyond).
}
\description{
Computes Euclidean distance from non-\code{NA} cells in a raster and returns a
binary mask where distance is within the threshold. Useful for constraining
analysis to a corridor around streams (e.g., \code{max_width} parameter in VCA).
}
\details{
Distance is computed with \code{\link[terra:distance]{terra::distance()}} which calculates Euclidean
distance from the nearest non-\code{NA} cell. Feature cells themselves receive
distance \code{0} and are always included in the mask.
}
