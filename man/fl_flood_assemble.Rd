% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fl_flood_assemble.R
\name{fl_flood_assemble}
\alias{fl_flood_assemble}
\title{Union multiple binary rasters}
\usage{
fl_flood_assemble(...)
}
\arguments{
\item{...}{Two or more \code{SpatRaster} objects with binary values, or a single
multi-layer \code{SpatRaster}.}
}
\value{
A \code{SpatRaster} with \code{1} where any input is \code{1}, \code{0} otherwise.
}
\description{
Combines multiple binary (0/1) rasters via logical OR. A cell is \code{1} if
it is \code{1} in any input layer. Useful for merging flood or floodplain layers
from different sources.
}
\examples{
slope <- terra::rast(system.file("testdata/slope.tif", package = "flooded"))

# Two masks with different thresholds
gentle <- fl_mask(slope, threshold = 5, operator = "<=")
moderate <- fl_mask(slope, threshold = 15, operator = "<=")

# Union: cells meeting either criterion
combined <- fl_flood_assemble(gentle, moderate)
terra::plot(combined, col = c("grey90", "steelblue"), main = "Assembled masks")

}
