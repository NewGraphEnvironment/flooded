% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fl_valley_confine.R
\name{fl_valley_confine}
\alias{fl_valley_confine}
\title{Delineate unconfined valleys using the Valley Confinement Algorithm}
\usage{
fl_valley_confine(
  dem,
  streams,
  area_field,
  slope = NULL,
  slope_threshold = 9,
  max_width = 2000,
  cost_threshold = 2500,
  flood_factor = 6,
  precip = NULL,
  waterbodies = NULL,
  channel_buffer = NULL,
  size_threshold = 5000,
  hole_threshold = 2500,
  field = NULL
)
}
\arguments{
\item{dem}{A \code{SpatRaster} of elevation.}

\item{streams}{An \code{sf} linestring object or a \code{SpatRaster} of rasterized
streams. If \code{sf}, it is rasterized using \code{area_field}. If a \code{SpatRaster},
its cell values are used as-is and must already be upstream contributing
area in hectares.}

\item{area_field}{Character. Column of \code{streams} holding \strong{upstream
contributing area in hectares}, rasterized onto the DEM grid by
\code{\link[=fl_stream_rasterize]{fl_stream_rasterize()}}. Required when \code{streams} is \code{sf} — there is no
default. The rasterized values become the drainage-area term of the
bankfull regression in \code{\link[=fl_flood_surface]{fl_flood_surface()}}, which accepts any positive
numeric column without complaint: channel width in place of area returns a
smaller floodplain with no error and no warning. Not used when \code{streams} is
already a \code{SpatRaster}. That branch cannot inspect the values it is handed;
it warns only when the layer's \emph{name} gives it away, so a raster burned from
any other wrong column carries the same defect one call earlier, undetected.}

\item{slope}{A \code{SpatRaster} of percent slope. If \code{NULL}, derived from \code{dem}.}

\item{slope_threshold}{Numeric. Maximum percent slope for valley floor.
Default \code{9}.}

\item{max_width}{Numeric. Maximum valley width in map units (metres).
Default \code{2000}.}

\item{cost_threshold}{Numeric. Maximum accumulated cost distance.
Default \code{2500}.}

\item{flood_factor}{Numeric. Multiplier on bankfull depth. Default \code{6}.}

\item{precip}{A \code{SpatRaster} or numeric scalar of mean annual precipitation
in \strong{millimetres}, converted to cm/yr internally for the bankfull
regression. Default \code{NULL}, which drops the precipitation term.}

\item{waterbodies}{An \code{sf} polygon object of lakes and/or wetlands, or
\code{NULL} (default). Waterbody polygons are rasterized onto the valley grid
and added to the output after morphological cleanup. No buffer is applied —
a lake or wetland in the valley is part of the flood system as-is.
No spatial filtering is applied — all polygons are rasterized. Pre-filter
to valley-bottom features before calling if headwater waterbodies are
not wanted.}

\item{channel_buffer}{Logical. Buffer streams by their \code{channel_width}
attribute and add to the valley output. Default \code{TRUE} when \code{streams} is
an \code{sf} object with a \code{channel_width} column, \code{FALSE} otherwise. The
stream channel is floodplain but can be sub-pixel at coarse DEM resolution.}

\item{size_threshold}{Numeric. Minimum valley patch area (m²). Default \code{5000}.}

\item{hole_threshold}{Numeric. Maximum hole area to fill (m²). Default \code{2500}.}

\item{field}{Deprecated. The former name of \code{area_field}, whose
\code{"channel_width"} default was wrong for the flood model (#47). Supplying it
warns and forwards to \code{area_field}; removal is tracked in flooded#53.}
}
\value{
A \code{SpatRaster} with binary values: \code{1} = unconfined valley, \code{0} =
confined / hillslope, \code{NA} = outside analysis extent.
}
\description{
Orchestrates the full VCA pipeline: slope thresholding, cost-distance
analysis, flood surface modelling, and morphological cleanup to identify
unconfined valley bottoms.
}
\details{
The algorithm combines four criteria via intersection (AND):
\enumerate{
\item \strong{Slope mask} — cells with slope <= \code{slope_threshold}
\item \strong{Distance mask} — cells within \code{max_width / 2} of a stream
\item \strong{Cost distance mask} — cells with accumulated cost < \code{cost_threshold}
\item \strong{Flood mask} — cells identified as flooded by bankfull regression
}

The combined mask then undergoes morphological cleanup:
\itemize{
\item Closing filter (3x3) to bridge small gaps
\item Fill small holes (< \code{hole_threshold})
\item Remove small patches (< \code{size_threshold})
\item Majority filter (3x3) to smooth edges
}

After cleanup, optional features are added via logical OR:
\itemize{
\item \strong{Channel buffer} — streams buffered by \code{channel_width} (DEM correction)
\item \strong{Waterbodies} — user-supplied lake/wetland polygons rasterized as-is
}

Adapted from the USDA Valley Confinement Algorithm Toolbox (BlueGeo
implementation by Devin Cairns, MIT license) and bcfishpass lateral
habitat assembly (Simon Norris, Apache 2.0).
\subsection{Performance}{

Several internal operations (focal filters, distance calculations, raster
math) support multi-threading via \code{\link[terra:terraOptions]{terra::terraOptions()}}. Set threads
before calling this function to speed up processing on large rasters:

\if{html}{\out{<div class="sourceCode">}}\preformatted{terra::terraOptions(threads = 12)
}\if{html}{\out{</div>}}

On an Apple M4 Max (16 cores), 12 threads reduced runtime from ~3.5
minutes to ~1 minute for a 27M-cell raster (~2,700 km² at 10 m).
}
}
\examples{
dem <- terra::rast(system.file("testdata/dem.tif", package = "flooded"))
streams <- sf::st_read(
  system.file("testdata/streams.gpkg", package = "flooded"),
  quiet = TRUE
)
precip_r <- fl_stream_rasterize(streams, dem, field = "map_upstream")

# Basic VCA (channel buffer auto-detected from streams$channel_width)
valleys <- fl_valley_confine(
  dem, streams,
  area_field = "upstream_area_ha",
  precip = precip_r
)
terra::plot(valleys, col = c("grey90", "darkgreen"), main = "Unconfined valleys")

# With waterbodies — fills lake/wetland donut holes
waterbodies <- sf::st_read(
  system.file("testdata/waterbodies.gpkg", package = "flooded"),
  quiet = TRUE
)
valleys_wb <- fl_valley_confine(
  dem, streams,
  area_field = "upstream_area_ha",
  precip = precip_r,
  waterbodies = waterbodies
)
terra::plot(valleys_wb, col = c("grey90", "darkgreen"), main = "With waterbodies")

}
\seealso{
\code{\link[=fl_stream_rasterize]{fl_stream_rasterize()}} for how \code{area_field} is burned onto the
grid, and \code{\link[=fl_flood_surface]{fl_flood_surface()}} for the regression that consumes it.
\code{\link[=fl_mask]{fl_mask()}}, \code{\link[=fl_cost_distance]{fl_cost_distance()}}, \code{\link[=fl_flood_model]{fl_flood_model()}}, \code{\link[=fl_patch_rm]{fl_patch_rm()}},
\code{\link[=fl_valley_poly]{fl_valley_poly()}}
}
