% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fl_flood_depth.R
\name{fl_flood_depth}
\alias{fl_flood_depth}
\title{Interpolate flood surface and compute depth above terrain}
\usage{
fl_flood_depth(dem, flood_surface, max_width = 2000, streams = NULL)
}
\arguments{
\item{dem}{A \code{SpatRaster} of elevation.}

\item{flood_surface}{A \code{SpatRaster} of flood surface elevation at stream
cells (output of \code{\link[=fl_flood_surface]{fl_flood_surface()}}). \code{NA} at non-stream cells.}

\item{max_width}{Numeric. Maximum corridor width in map units (metres)
within which to interpolate. Default \code{2000} (1000m each side).}

\item{streams}{A \code{SpatRaster} of rasterized streams used to define the
interpolation corridor. If \code{NULL}, derived from non-\code{NA} cells in
\code{flood_surface}.}
}
\value{
A \code{SpatRaster} of flood depth (metres above terrain). Positive
values are flooded; \code{0} at stream cells; \code{NA} outside the corridor or
where depth is negative (terrain above flood surface).
}
\description{
Takes the flood surface elevation at stream cells (from
\code{\link[=fl_flood_surface]{fl_flood_surface()}}) and interpolates it outward to produce a continuous
water surface, then subtracts the DEM to get flood depth. Positive values
indicate flooding.
}
\details{
Interpolation uses \code{\link[terra:interpIDW]{terra::interpIDW()}} (inverse distance weighting) to
propagate the flood surface from stream cells outward. This differs from
the Python VCA which uses \code{scipy.interpolate.griddata} with linear
interpolation — IDW is available natively in terra and produces similar
results for this application.

The interpolation domain is limited to cells within \code{max_width / 2} of
the nearest stream cell to avoid extrapolating into distant terrain.
}
